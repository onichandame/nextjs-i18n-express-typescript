# Template NextJS Project

a template next.js project with i18n, express server and ts configured

# Scripts

start dev server:

```bash
yarn dev
```

build to /dist:

```bash
yarn build
```

start built server

```bash
yarn start
```
